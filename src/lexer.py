# coding=utf-8

from regexp import reader

# сначала разбить текст на слова, числа и символы
# с помощью комбинации str.join() и str.split()
# затем перевести их в токены


def lexer(code):
    assert isinstance(code, list)
    pass
